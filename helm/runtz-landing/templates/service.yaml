apiVersion: v1
kind: Service
metadata:
  name: {{ include "runtz-landing.fullname" . }}
  labels:
    {{- include "runtz-landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "runtz-landing.selectorLabels" . | nindent 4 }}
