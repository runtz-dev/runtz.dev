runtz landing has been installed.

Landing pages:
  https://runtz.dev

Internal service:
  {{ include "runtz-landing.fullname" . }}:{{ .Values.service.port }}

Image:
  {{ .Values.image.repository }}:{{ .Values.image.tag }}
